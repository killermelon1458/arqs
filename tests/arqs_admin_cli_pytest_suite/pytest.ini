[pytest]
addopts = -ra -q -p no:cacheprovider
norecursedirs = .pytest_cache .pytest_admin_cli_tmp pytest-cache-files*
markers =
    cli: admin CLI command and output tests
    ip: IP access policy and rule lifecycle tests
    cleanup: cleanup and expiry behavior tests
    compat: compatibility and legacy-entrypoint tests
