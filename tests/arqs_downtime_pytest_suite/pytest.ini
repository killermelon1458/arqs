[pytest]
addopts = -ra -v
markers =
    recovery: crash/restart/downtime recovery tests
    replay: packet-id replay / dedupe behavior tests
    admin: admin-state semantics tests
    docker: tests that stop/start/kill the Docker container
